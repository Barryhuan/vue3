export default {
  pointFocusAreaInfo({ pointFocusAreaInfo }) {
    return pointFocusAreaInfo
  },
  pointTaskInfo({ pointTaskInfo }) {
    return pointTaskInfo
  },
  pointCenter({ pointCenter }) {
    return pointCenter
  },
  user({ user }) {
    return user
  },
  pointId( { pointId } ) {
    return pointId
  },
  pointList({ pointList }) {
    return pointList
  },
  cityId({ cityId }) {
    return cityId
  },
  cityList({ cityList }) {
    return cityList
  },
  loadingStatus({ loadingStatus }) {
    return loadingStatus
  }
}